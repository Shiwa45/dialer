# calls/admin.py
from django.contrib import admin
# Add your call models when ready
# from .models import CallLog, CallRecording

# @admin.register(CallLog)
# class CallLogAdmin(admin.ModelAdmin):
#     list_display = ['phone', 'campaign', 'agent', 'start_time', 'duration', 'disposition']
#     list_filter = ['start_time', 'disposition']
#     search_fields = ['phone', 'campaign__name', 'agent__username']

# =============================================================================

