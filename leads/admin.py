# leads/admin.py
from django.contrib import admin
# Uncomment and add your lead models when ready
# from .models import Lead, LeadList, LeadFilter, DNCList

# @admin.register(Lead)
# class LeadAdmin(admin.ModelAdmin):
#     list_display = ['first_name', 'last_name', 'phone', 'status', 'created_at']
#     list_filter = ['status', 'created_at']
#     search_fields = ['first_name', 'last_name', 'phone', 'email']

# @admin.register(LeadList)
# class LeadListAdmin(admin.ModelAdmin):
#     list_display = ['name', 'total_leads', 'is_active', 'created_at']
#     list_filter = ['is_active', 'created_at']
#     search_fields = ['name', 'description']

# =============================================================================

