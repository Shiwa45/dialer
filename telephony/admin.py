# telephony/admin.py
from django.contrib import admin
# Add your telephony models when ready
# from .models import AsteriskServer, Carrier, DID, IVR

# @admin.register(AsteriskServer)
# class AsteriskServerAdmin(admin.ModelAdmin):
#     list_display = ['name', 'ip_address', 'port', 'is_active']
#     list_filter = ['is_active']
#     search_fields = ['name', 'ip_address']

# =============================================================================

# agents/admin.py
